# 使用方法：
# 1. 复制本文件，改名为 .env（注意前面有点，没有 .txt）
#    若记事本保存成了 .env.txt，也可以——后端会自动读取。
# 2. 把下面等号右边换成你在 Reverb 后台生成的 Personal Access Token
# 3. 保存。.env 已被 Git 忽略，不会随仓库上传。
#
# REVERB_TOKEN=粘贴_token_不要加引号一般即可

REVERB_TOKEN=
